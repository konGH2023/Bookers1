<h1>Books</h1>
<%= @books.each do |book| %>
<table>
  <div>title</div>
  <div>gaiyou</div>
  <div>
    <a hrfe="">show</a>
  </div>
  <div>
    <a hrfe="">edit</a>
  </div>
  <div>
    <a hrfe="">destroy</a>
  </div>
</table>
<% end %>
<h2>New book</h2>

<%= form_with model: Book.new do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>