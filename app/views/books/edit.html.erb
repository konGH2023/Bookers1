<h1>Editing Book</h1>

<%= form_with model: @book, url: 'book.id', method: :patch do |f| %>
<!-- %= form_with model: Book.new do |f| % -->
  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>
<%= link_to "Edit", show_book_path(@Book.id) %>
<%= link_to "Back", edit_book_path(@Book.id) %>